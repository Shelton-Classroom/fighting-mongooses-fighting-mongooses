@model IEnumerable<mongoose.Models.Saved_Internship>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Internship.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Internship.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Saved_InternshipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Saved_InternshipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Saved_InternshipId })
        </td>
    </tr>
}

</table>
