@model mongoose.Models.Student_Course

@{
    ViewBag.Title = "Details";
}
@{ if (User.IsInRole("Employer"))
    {
        @Html.Partial("~/Areas/EmployerSection/Views/Shared/_employerNav.cshtml")
    }
    if (User.IsInRole("Student"))
    {
        @Html.Partial("~/Areas/StudentSection/Views/Shared/_studentNav.cshtml")
    }
    if (User.IsInRole("Instructor"))
    {
        @Html.Partial("~/Areas/InstructorSection/Views/Shared/_instructorNav.cshtml")
    }
    if (User.IsInRole("Admin"))
    {
        @Html.Partial("~/Areas/AdminSection/Views/Shared/_adminNav.cshtml")
    }
}
<h2>Details</h2>

<div>
    <h4>Student_Course</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SemesterCompleted)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SemesterCompleted)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Grade)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Grade)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Term)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Term)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Cours.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Cours.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Student.FirstName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Student.FirstName)
        </dd>

    </dl>
</div>

<p>
    @{
        if (User.IsInRole("Student"))
        {
            @Html.ActionLink("Back", null, null, null, new { href = Request.UrlReferrer })
        }
        if (User.IsInRole("Employer"))
        {
            @Html.ActionLink("Back", null, null, null, new { href = Request.UrlReferrer })
        }
        if (User.IsInRole("Instructor"))
        {
            @Html.ActionLink("Back", null, null, null, new { href = Request.UrlReferrer })

        }
        if (User.IsInRole("Admin"))
        {
            <div>
                @Html.ActionLink("Edit", "Edit", new { id = Model.StudentCourseid }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Back", "Index", null, new { @class = "btn btn-success" })
            </div>

        }
    }
</p>
