@model mongoose.Models.Major

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Major</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.RequiredCredits)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RequiredCredits)
        </dd>

    </dl>
</div>
<p>
    @{
        if (User.IsInRole("Student"))
        {
            @Html.ActionLink("Back", null, null, null, new { href = Request.UrlReferrer })
        }
        if (User.IsInRole("Employer"))
        {
            @Html.ActionLink("Back", null, null, null, new { href = Request.UrlReferrer })
        }
        if (User.IsInRole("Instructor"))
        {
            @Html.ActionLink("Edit", "Edit", new { id = Model.MajorId })
            @Html.ActionLink("Back to Majors", "Classes", "Instructors", new { area = "InstructorSection" }, null)
        }
    }
</p>
